include _sim_shared.pug

section#projects(class="scroll-mt-20 px-4 pb-16 pt-10 sm:px-6 print:hidden")
  div(class="mx-auto max-w-6xl")
    header(class="mb-10 max-w-3xl")
      p(class="text-xs font-bold uppercase tracking-widest text-resume-muted")= t('projects.kicker')
      h2(class="mt-1 text-3xl font-extrabold tracking-tight text-resume-ink")= t('projects.title')
      p(class="mt-3 text-sm leading-relaxed text-resume-body")= t('projects.intro')
    include _sim_learning.pug
    include _sim_meeting.pug
    include _sim_haloki.pug
